# @package _global_
defaults:
  - /base
  - /algo: ppo
  - /env: cartpole
  - /redo: "off"
  - _self_

seed: 1

instrumentation:
  enabled: true
  probe_batch_size: 512
  metric_every_updates: 200
  heavy_metric_every_updates: 1000

