# configs/experiment/dqn_cartpole_baseline.yaml
defaults:
  - /base
  - /algo/dqn
  - /env/cartpole
  - /redo/off

seed: 1

instrumentation:
  enabled: true
  probe_batch_size: 512
  metric_every_updates: 500
  heavy_metric_every_updates: 2000

run:
  total_env_steps: 500000