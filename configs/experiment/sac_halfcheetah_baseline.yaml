# @package _global_
defaults:
  - /base
  - /algo: sac
  - /env: halfcheetah
  - /redo: "off"
  - _self_

seed: 1

instrumentation:
  enabled: true
  probe_batch_size: 512
  metric_every_updates: 500
  heavy_metric_every_updates: 2000

#Added
run:
  total_env_steps: 200000